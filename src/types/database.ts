// ─────────────────────────────────────────────────────────────────────────────
// Database type stub
// Full types are generated via `supabase gen types typescript` once the
// Supabase CLI is connected. Using `any` prevents type inference errors
// on queries while we build out the schema. All repository functions
// return explicit types from @/types — they do NOT rely on this for correctness.
//
// After connecting the Supabase CLI, run:
//   npx supabase gen types typescript --linked > src/types/supabase.ts
// Then replace this file's content with the generated types.
// ─────────────────────────────────────────────────────────────────────────────

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
